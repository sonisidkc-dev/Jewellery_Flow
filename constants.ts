import { STAGES } from './types';

export { STAGES };

export const APP_KEYS = {
  JOBS: 'jewel_flow_jobs',
  USERS: 'jewel_flow_users',
  LOGS: 'jewel_flow_logs',
  DAILY: 'jewel_flow_daily',
  SESSION: 'jewel_flow_session'
};